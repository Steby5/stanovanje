{
  "_comment": "Copy to subscribers.json to run fbwatch for more than one person. Delete it (or never create it) and fbwatch runs single-user off config.json + keywords.txt. Each person gets their own trigger-word file and their own destination; Facebook is still scraped only once per cycle.",

  "domin": {
    "enabled": true,
    "admin": true,
    "keywords_file": "keywords.txt",
    "discord_webhook_url": "https://discord.com/api/webhooks/XXXX/YYYY",
    "discord_user_id": "",
    "_note": "admin: may add people and groups from Discord. discord_user_id links a Discord account so this person can manage their own rules with `!fbw add ...`."
  },

  "ana": {
    "enabled": true,
    "keywords_file": "keywords/ana.txt",
    "discord_webhook_url": "https://discord.com/api/webhooks/AAAA/BBBB",
    "discord_user_id": "",
    "_note": "Own webhook, own trigger words. Gets nothing until keywords/ana.txt has at least one rule."
  },

  "bo": {
    "enabled": true,
    "keywords_file": "keywords/bo.txt",
    "discord_channel_id": "1234567890123456789",
    "discord_user_id": "222222222222222222",
    "mention": true,
    "_note": "No webhook needed: the bot posts straight into this channel, so nothing secret sits in this file. Requires discord_bot_token in config.json and the bot in that channel with View Channel + Send Messages + Embed Links. Two people with the SAME channel id (or the same webhook) share one message that @-mentions each of them, instead of the post arriving twice. Set mention:false to receive posts without being pinged."
  },

  "marko": {
    "enabled": true,
    "keywords_file": "keywords/marko.txt",
    "telegram_chat_id": "123456789",
    "groups": ["504531176006435"],
    "_note": "Telegram instead of Discord, and only this one group. Leave `groups` out to watch all of them. Telegram is notifications-only - configuration stays in Discord."
  }
}
